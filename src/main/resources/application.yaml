spring:
  application:
    name: high-demand-ticketing-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/high_demand_ticketing_db
    username: postgres
    password: postgres

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
